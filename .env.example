# Frontend (raíz del proyecto)
# Puerto en el que corre la app React (npm start)
PORT=3003

# URL del API. Por defecto usa /api que está configurado en nginx para hacer proxy al backend.
# Esto evita errores de contenido mixto (HTTPS -> HTTP) cuando la app se sirve con HTTPS.
# REACT_APP_API_URL=/api
