# Ejemplo de configuración nginx para sistemas.mopc.gov.py
# =========================================================
# Este archivo es solo REFERENCIA. No reemplaza la config real del servidor.
#
# Dónde suele estar nginx en el servidor:
#   - Linux: /etc/nginx/nginx.conf  o  /etc/nginx/sites-available/default
#   - El bloque "server" que ya tenga SSL para sistemas.mopc.gov.py es donde
#     hay que agregar el "location /api/" que está más abajo.
#
# Quién debe editarlo: alguien con acceso root/sudo al servidor donde corre
# https://sistemas.mopc.gov.py (puede ser otro servidor, no necesariamente 172.16.222.222).

# --- Ejemplo de bloque server (solo como guía) ---
# server {
#     listen 443 ssl;
#     server_name sistemas.mopc.gov.py;
#     ssl_certificate     /ruta/a/certificado.crt;
#     ssl_certificate_key /ruta/a/privada.key;
#
#     # Donde se sirve el frontend (monitoreo_vmt)
#     location /monitoreo_vmt/ {
#         alias /ruta/donde/esta/el/build/;
#         try_files $uri $uri/ /monitoreo_vmt/index.html;
#     }
#
#     # AGREGAR ESTE BLOQUE: proxy del API al backend
#     location /api/ {
#         proxy_pass http://172.16.222.222:8010/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

# --- Solo el bloque que hay que agregar (copiar esto dentro del server de sistemas.mopc.gov.py) ---

location /api/ {
    proxy_pass http://172.16.222.222:8010/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
